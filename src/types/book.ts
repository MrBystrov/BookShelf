import { watch } from "vue";

export interface IBook {
    id: number, 
    title: string,
    author: string,
    publication_year: number,
    genre: string[]
    description: string,
    cover_image: string
}

export interface IPlanBook {
    id: number,
}







